##This file matters coz... 
#Locks Terraform version
#Locks Google provider version
#Sets up both google and google‑beta (needed for some GKE + Redis features)
#Reads project_id and region from variables (we’ll define them next)

terraform {
  required_version = ">= 1.6.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 5.0"
    }
  }
}

provider "google" {
  project = var.project_id
  region  = var.region
}

provider "google-beta" {
  project = var.project_id
  region  = var.region
}